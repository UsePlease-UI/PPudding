import { css } from '@emotion/react';

export const iconButtonStyle = {
    iconButton: css({
        width: 'max-content',
        height: 'max-content',
        borderRadius: 4,
        '& svg': {
            display: 'block'
        }
    })
};
