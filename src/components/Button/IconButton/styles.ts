import { css } from '@emotion/react';

export const iconButtonStyle = {
    iconButton: css({
        width: 'max-content',
        height: 'max-content',
        fontFamily: 'inherit',
        '& svg': {
            display: 'block'
        }
    })
};
