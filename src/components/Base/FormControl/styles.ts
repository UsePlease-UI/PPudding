import { css } from '@emotion/react';

export const formControlStyle = {
    container: css({
        width: '100%'
    }),
    labelText: css({
        display: 'block'
    }),
    helperText: css({
        display: 'block',
        marginTop: 4
    })
};
